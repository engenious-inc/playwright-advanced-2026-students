// GENERATED by scripts/gen-framework.mjs from specs/m08-tubi-livetv.manifest.json.
// Do NOT hand-edit — change the manifest and run `npm run gen:framework`.

// Anonymous Live TV browse needs no data seeding; the seed asserts the anchor site is reachable before the journey suite runs. Excluded from normal runs via testIgnore ['**/seed.spec.ts'].
import { test, expect } from '../fixtures.js';

test('seed: anchor site is reachable before the suite', async ({ page }) => {
  const response = await page.goto('/');
  expect(response?.ok()).toBeTruthy();
});
