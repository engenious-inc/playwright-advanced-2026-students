// GENERATED by scripts/gen-framework.mjs from specs/m08-tubi-livetv.manifest.json.
// Do NOT hand-edit — change the manifest and run `npm run gen:framework`.

// specSource: manifest journey "live-tv.reachable"
// requirement: Live TV is reachable from the home page
// risk: Users cannot find Live TV -> lost live-ad revenue
// owner: live-experience · layer: ui · ciTier: smoke
import { test, expect } from '../../fixtures.js';

test('Live TV is reachable from the home page', async ({ page, tubiLiveTv }) => {
  await tubiLiveTv.goto();
  // step 1: open Live TV from the home menubar
  await tubiLiveTv.openLiveTv();
  // evidence: URL matches /live and the category nav stays visible
  await expect(page).toHaveURL(/\/live/);
  await expect(tubiLiveTv.categoryNav()).toBeVisible();
});
