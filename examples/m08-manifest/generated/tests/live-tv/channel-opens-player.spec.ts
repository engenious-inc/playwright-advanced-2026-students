// GENERATED by scripts/gen-framework.mjs from specs/m08-tubi-livetv.manifest.json.
// Do NOT hand-edit — change the manifest and run `npm run gen:framework`.

// specSource: manifest journey "live-tv.channel-opens-player"
// requirement: Clicking a channel opens the player
// risk: Channel click dead-ends -> no playback, core product failure
// owner: playback · layer: ui · ciTier: smoke
import { test, expect } from '../../fixtures.js';

test('Clicking a channel opens the player', async ({ page, tubiLiveTv }) => {
  await tubiLiveTv.goto();
  // step 1: open Live TV from the home menubar
  await tubiLiveTv.openLiveTv();
  // step 2: click the channel row at the given index
  await tubiLiveTv.openChannel(0);
  // evidence: URL matches /live/<id> and the video element is visible
  await expect(page).toHaveURL(/\/live\/\d+/);
  await expect(tubiLiveTv.player()).toBeVisible();
});
