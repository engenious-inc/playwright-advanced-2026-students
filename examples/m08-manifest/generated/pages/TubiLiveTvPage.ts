// GENERATED by scripts/gen-framework.mjs from specs/m08-tubi-livetv.manifest.json.
// Do NOT hand-edit — change the manifest and run `npm run gen:framework`.

import type { Locator } from '@playwright/test';
import { BasePage } from '../../../../shared/BasePage.js';

export class TubiLiveTvPage extends BasePage {
  readonly path = '/live';

  categoryNav(): Locator {
    return this.page.getByRole('navigation', { name: 'Categories' });
  }

  channelRows(): Locator {
    return this.page.getByTestId('channel-list-item');
  }

  player(): Locator {
    // no semantic role; the video element is the player surface
    return this.page.locator('video');
  }

  async openLiveTv(): Promise<void> {
    // step: open Live TV from the home menubar
    await this.categoryNav().getByText('Live TV').click();
  }

  async openChannel(index: number): Promise<void> {
    // step: click the channel row at the given index
    await this.channelRows().nth(index).click();
  }
}
